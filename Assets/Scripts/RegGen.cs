using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RegGen : MonoBehaviour
{
    /*int firstRun = 0; // variable inside the class, but not inside a function.

    void Start()
    {
        firstRun = PlayerPrefs.GetInt("savedFirstRun");


        if (firstRun == 0)
        {
            PlayerPrefs.SetString("BestTimeEasy", "PH");
            PlayerPrefs.SetString("BestTimeNormal", "PH");
            PlayerPrefs.SetString("BestTimeHard", "PH");
            firstRun = 1;
        }
        else
        {
            

        }

    }*/
}